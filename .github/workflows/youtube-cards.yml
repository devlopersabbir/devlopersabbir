name: GitHub Readme YouTube Cards
on:
  schedule:
    # Runs every hour, on the hour
    - cron: "0 * * * *"
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: devlopersabbir/devlopersabbir
        with:
          channel_id: UC-kwgB_vfZlCtI_eXijNhMw
